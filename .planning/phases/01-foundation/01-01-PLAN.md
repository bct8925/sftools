---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, package-lock.json]
autonomous: true
---

<objective>
Install the @salesforce-ux/icons npm package and verify the import path structure for use in Phase 01-02.

Purpose: Establish the SLDS icons dependency and document the correct import paths for Vite's ?raw imports.
Output: Package installed, verified path structure documented in SUMMARY.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @salesforce-ux/icons package</name>
  <files>package.json, package-lock.json</files>
  <action>
Run `npm install @salesforce-ux/icons` to add the package as a dependency.

Do NOT install @salesforce-ux/design-system - it's larger and includes CSS/tokens we don't need. The icons package alone provides all SVG files.
  </action>
  <verify>
- package.json contains @salesforce-ux/icons in dependencies
- npm ls @salesforce-ux/icons shows installed version
  </verify>
  <done>Package installed, visible in package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Verify icon path structure</name>
  <files>none (exploration only)</files>
  <action>
Explore the installed package to determine exact import paths:

1. Check node_modules/@salesforce-ux/icons/dist/svg/ structure
2. Identify the utility icons folder (expected: dist/svg/utility/)
3. Verify a few icon files exist: close.svg, refresh.svg, edit.svg, delete.svg
4. Test that Vite can import one via ?raw suffix by checking file content

The research suggests paths like:
- @salesforce-ux/icons/dist/svg/utility/close.svg

But this needs verification - there may be subfolders like ltr/rtl or common/.

Document the exact working path pattern for use in Plan 01-02.
  </action>
  <verify>
- Can list SVG files in the utility icons folder
- Path structure matches expected @salesforce-ux/icons/dist/svg/{category}/{icon}.svg pattern
  </verify>
  <done>Exact import path documented (e.g., "@salesforce-ux/icons/dist/svg/utility/close.svg")</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm ls @salesforce-ux/icons shows package installed
- [ ] Utility icons folder exists with expected SVG files
- [ ] Import path pattern verified and documented
</verification>

<success_criteria>
- @salesforce-ux/icons package installed
- Exact import path structure known
- No build errors from npm install
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with:
- Package version installed
- Verified import path pattern
- List of available icon categories
- Any surprises or adjustments needed from research expectations
</output>
