---
phase: 02-icon-replacement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/icons.js]
autonomous: true
---

<objective>
Extend icons.js with SLDS icons needed for button-icon component replacements.

Purpose: Add clock and settings icons to the SLDS icon library so button-icon components can use them instead of HTML entities.
Output: Updated icons.js with 2 new SLDS icon imports.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@src/lib/icons.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map HTML entities to SLDS icons</name>
  <files>n/a</files>
  <action>
  Identify the SLDS equivalents for button-icon HTML entities currently in use:

  | HTML Entity | Unicode | Visual | SLDS Icon |
  |-------------|---------|--------|-----------|
  | &#128337;   | U+1F551 | 🕑     | clock.svg |
  | &#9881;     | U+2699  | ⚙      | settings.svg |
  | &#8635;     | U+21BB  | ↻      | refresh.svg (already imported) |
  | &#8942;     | U+22EE  | ⋮      | threedots_vertical.svg (already imported) |

  Only clock and settings need to be added.
  </action>
  <verify>Manual review of mapping</verify>
  <done>Mapping documented</done>
</task>

<task type="auto">
  <name>Task 2: Add clock and settings icons to icons.js</name>
  <files>src/lib/icons.js</files>
  <action>
  Import clock and settings SVGs from @salesforce-ux/icons using the established pattern:

  1. Add imports after existing imports:
     ```javascript
     import clockIcon from '@salesforce-ux/icons/dist/salesforce-lightning-design-system-icons/utility/clock.svg?raw';
     import settingsIcon from '@salesforce-ux/icons/dist/salesforce-lightning-design-system-icons/utility/settings.svg?raw';
     ```

  2. Add to icons object with appropriate sizes:
     ```javascript
     clock: processSvg(clockIcon, { size: 16 }),
     settings: processSvg(settingsIcon, { size: 16 }),
     ```

  Use the same processSvg helper that applies currentColor for theme inheritance.
  </action>
  <verify>npm run build succeeds without errors</verify>
  <done>icons.clock and icons.settings are exported and usable</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] icons.js exports clock and settings
- [ ] No TypeScript/lint errors
</verification>

<success_criteria>

- All tasks completed
- icons.js contains 8 unique SLDS icon imports (6 existing + 2 new)
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-icon-replacement/02-01-SUMMARY.md`
</output>
